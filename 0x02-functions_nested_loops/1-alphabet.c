#include "main.h"

/**
 *
 * main - prints alphabets in lowercase
 *
 * Return: Always return 0
 */
int main(void)
{

	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);

	_putchar('\n');

	return (0);

}
