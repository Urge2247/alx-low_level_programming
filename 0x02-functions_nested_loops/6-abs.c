#include "main.h"
#include <stdio.h>
/**
 * _abs - prints abs of a number
 *@c - num to be passed to fun
 * Return: 0
 */
int _abs(int)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
		return (c * -1);
}
