#include "main.h"
#include <stdio.h>

/**
 * main - prints positive or negative integer
 * @i: an int
 * Return: Always return 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
