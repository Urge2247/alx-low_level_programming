#include <stdio.h>
#include "main.h"
/**
 * print_numbers - prints numbers from 0 to 9 followed
 * @i: int to be added to fun
 * Return: nothing
 */
void print_numbers(void)
{
	int i;

	for (i = 28; i < 38; i++)
	{
		putchar(i);
	}

	putchar(10);
}
