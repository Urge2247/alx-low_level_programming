#include <stdio.h>
#include "main.h"
/**
 * print_numbers - prints numbers from 0 to 9 followed
 * Return: nothing
 */
void print_numbers(void)
{
	int i =28;

	while (i <= 38)
	{
		putchar(i);
		i++;
	}

	putchar(10);
}
