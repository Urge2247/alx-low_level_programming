#include <stdio.h>
#include "main.h"
/**
 * print_numbers - prints numbers from 0 to 9 followed
 * @n: int to be added to fun
 * Return: nothing
 */
void print_numbers(void)
{
	int n;

	for (n = 28; n < 38; n++)
	{
		putchar(n);
	}

	putchar(10);
}
