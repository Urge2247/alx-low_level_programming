#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_strlen - prints length of string
 *@str: a char param
 *@s: a char to be added to fun
 *Return: 0
 */
int _strlen(char *s)
{
	char *str;
	int len;

	_strlen(s);
	len = _strlen(str);
	return (0);
}
